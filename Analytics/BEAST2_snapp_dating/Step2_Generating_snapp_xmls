## NOTE1: This script should be run from the directory containing the VCF output from BEAST2_snapp_dating/Step1.
## NOTE2: This script generates two xml files based on different clibartion approaches. 
## NOTE3: This script uses constraint files (see Step2_constaint18 & Step2_constaint19). Herein they are called "constraint18" and "constraint19"
## NOTE4: This script uses a starting tree (see Step2_alba.tre). Herein they are called "alba.tre"
## NOTE5: This script requires an accoumpanying ruby script in the directory where it is initiated. To download see https://raw.githubusercontent.com/mmatschiner/snapp_prep/master/snapp_prep.rb

#Change permissions
chmod -R 777 *

#Using ruby script to prepare xmls for BEAST
ruby snapp_prep.rb -v intersub_VQSRpassSNPs_10k_mergedNoUnWZ_maf00202_ldprune_variants_BEAST_test3.vcf -t samples36.txt -c constraint18.txt -s alba.tre -m 10000 -l 1000000 -x SNAPP_18.xml
ruby snapp_prep.rb -v intersub_VQSRpassSNPs_10k_mergedNoUnWZ_maf00202_ldprune_variants_BEAST_test3.vcf -t samples36.txt -c constraint19.txt -s alba.tre -m 10000 -l 1000000 -x SNAPP_19.xml

#Change permissions
chmod -R 777 *
