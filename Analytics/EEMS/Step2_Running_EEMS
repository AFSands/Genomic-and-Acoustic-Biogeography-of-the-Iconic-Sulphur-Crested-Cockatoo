## NOTE1: EEMS was run in an interactive node using the following parameter file and line of code (see below).
## NOTE2: This code was used to run two independent runs (i.e. run twice) with separate seed values.
## NOTE3: Preparing .coord, .outer and .diffs (from Plink .bed, .bim and .fam files) files can be found at (https://github.com/dipetkov/eems)
#########################################################################

runeems_snps --params run_eems_galerita10k_1.ini --seed 123
